package Settings

import user.Profile


internal class UserSettings {
     var profile = Profile()
    // farklı bir paket içerisinde olduğundan import etsek dahi çağıramıyoruz.
    // Internal class olduğundan dolayı

}